// Zed settings
//
// For information on how to configure Zed, see the Zed
// documentation: https://zed.dev/docs/configuring-zed
//
// To see all of Zed's default settings without changing your
// custom settings, run `zed: open default settings` from the
// command palette (cmd-shift-p / ctrl-shift-p)
{
  // Font sizes are left to the defaults (measured to match IntelliJ IDEA —
  // see `buffer_font_size` in the default settings). Uncomment to override;
  // a value pinned here wins over the default forever, including after the
  // default changes.
  // "ui_font_size": 14,
  // "buffer_font_size": 14,
  "theme": {
    "mode": "system",
    "light": "One Light",
    "dark": "One Dark",
  },
}
