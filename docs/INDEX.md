# docs/INDEX.md — documentation map

> The project's bookshelf. Every development session starts here (after
> `CLAUDE.md`).
>
> When adding a new doc — add a row in the appropriate table below.

---

## Entry points

- **Starting a new session?** → read `CLAUDE.md` + this file.
- **About to dispatch a sub-agent or plan a feature?** → [`workflow/supervisor-mode.md`](workflow/supervisor-mode.md).
- **Not sure where to write something?** → [`workflow/doc-discipline.md`](workflow/doc-discipline.md).
- **What's different from upstream Zed?** → [`../FORK.md`](../FORK.md).

---

## workflow/

How sessions are run.

- [`workflow/supervisor-mode.md`](workflow/supervisor-mode.md) — the supervisor's playbook (READ → DECIDE → DISPATCH → VERIFY → FINALIZE).
- [`workflow/doc-discipline.md`](workflow/doc-discipline.md) — when to create / update which doc.
- [`workflow/adr-template.md`](workflow/adr-template.md) — template for new ADRs.

---

## architecture/decisions/ — ADRs

Architectural decisions with long-term consequences (data formats, public-API
contracts, multi-crate invariants). Each ADR is dated `accepted`/`superseded`.

| # | Title | Status | Document |
|---|---|---|---|
| 0001 | Fork philosophy: no scheduled upstream merge | accepted | [`architecture/decisions/0001-fork-philosophy.md`](architecture/decisions/0001-fork-philosophy.md) |
| 0002 | Native headless GPUI platform for autonomous agent driving | accepted | [`architecture/decisions/0002-native-headless-platform.md`](architecture/decisions/0002-native-headless-platform.md) |
| 0003 | Remote Control transport — WebSocket over TLS, fingerprint-pinned, secret-authenticated | accepted | [`architecture/decisions/0003-remote-control-protocol.md`](architecture/decisions/0003-remote-control-protocol.md) |

---

## plans/ — HEAVY-track plan docs (committed)

Per-phase specs (acceptance criteria + verification + commit log). Filename
format: `YYYY-MM-DD-<slug>.md`. Status flips from `ready to dispatch` →
`in progress` → `complete`/`cancelled`.

These are **committed to the repo** so sub-agents dispatched in a worktree
can read them.

| Date | Status | Plan |
|---|---|---|
| 2026-08-30 | complete | [`plans/2026-08-30-entries-persist-chain-teardown.md`](plans/2026-08-30-entries-persist-chain-teardown.md) — closing a chat tab or a Solution window was silently and **permanently** losing the session's in-flight transcript writes. `entries_persist_chain` serialises per-session entry-row writes (GPUI detached tasks have no FIFO guarantee — the phase-6b keystone bug) by moving each link's predecessor `Task` into its own future, so the map entry owns the whole chain and `evict_session_runtime_maps`'s unconditional `.remove()` cancelled all of it; `persist_all_rows` / `persist_main_stream` advance `persisted_main_seq` synchronously *before* they spawn and every persist filters `mod_seq > watermark`, so no later persist re-picks those rows. Recon settled the question that had blocked the item for two sessions: the "legacy row layout" was **not** the hazard it was thought to be (the cold-load realign is already shipped, tested and intended — `store/tests/hydration.rs::legacy_teammate_tagged_rows_realign_to_main_local_on_cold_load`), and the real blocker was that `evict_session_runtime_maps` is shared between a soft close and a hard purge, with `purge_session_hard` evicting **before** it deletes — so a generic detach resurrects entry rows for a session with no `solution_sessions` row, orphans nothing enumerates (`delete_by_solution` selects via `session_id IN (SELECT id FROM solution_sessions WHERE solution_id = ?)`) and no reaper reaps. Hence `ChainDisposition::{Drain, Abandon}`, **stated by each caller, never inferred inside the evictor** — plus `cold_close_solution`'s `acp_thread().is_some()` liveness gate on `close_session`, required in the same change because the two bugs point in opposite directions (loss of writes that should happen vs. execution of a full-Main rewrite that should not, against a session the editor had merely restored). The first cut drained by `.detach()` and **shipped a Critical**: freeing the map key lets a close→reopen build a second chain with nothing ordering it against the first, so the close flush's trailing `delete_entries_from(old_main_len)` lands after the new chain's tail upsert and deletes the user's new message — measured A/B, 3-entry transcript closed, reopened and appended to = **4 rows on `53d8acb420`, 3 on `76be3e00fa`**; 200 entries with five reopen round trips = 201 vs 200. Fixed by **retention**: `Drain` removes nothing, so the reopen's first persist takes the flush as its `prev` for free and the deferred-eviction race never arises. The unbounded map is bounded without a generation counter — each link flips an `Arc<AtomicBool>` (`Release`, no `.await` after it) as its last act and `retire_finished_persist_chains` `retain`s on the paired `Acquire` load, safe because the map always holds the **outermost** link, so `finished` there implies every predecessor completed, and the `Release`/`Acquire` pairing carries the guarantee across threads (the chain runs on the **background** executor — see FORK.md #103). Retention created a hazard class the detach design did not have — a chain now outlives its session — closed by abandoning explicitly at the two purge paths that never reach `teardown_session_runtime` (`purge_session_hard`'s not-hydrated early return, and `purge_solution_fully`, which iterates only hydrated sessions; `PersistChain` carries its `solution_id` for the latter). `53d8acb420` `76be3e00fa` `f851e02f97` `92c52062e9`. **Task 2 (docs + review follow-ups)** added FORK.md decision #101, tightened `a_finished_persist_chain_is_retired_from_the_map` to assert the *negative* (a live chain survives the sweep — an unconditional-retire mutation now dies on that assertion by design instead of incidentally on the next one) and widened `purge_after_soft_close_abandons_the_drained_chain` with a depth-independent check that the not-hydrated branch reaches the chain at all. Deferred with recon detail rather than fixed: orphan entry rows survive a hard purge and nothing reaps them (cancellation walks inward one runnable at a time — 2 links leak 1 row, 8 leak 5 — and closing it needs a purge-ordering change); a reopen inside the flush window can still read a torn prefix (reproduced at 37 entries lost, but A/B proves it identical to the parent commit in every timing, so it is not a regression); a batched `upsert_entries` that would write the flush under one connection lock and shrink that window ~200x; and app quit, which at the time had no `on_app_quit` hook in `solution_agent` at all — since closed by FORK.md #103 (chain moved to the background executor + a quit hook that drains it, including the flush `App::shutdown`'s own `flush_effects` produces). Gates: **705 tests / 0 failed**, `cargo fmt --all --check` clean, scoped `./script/clippy` clean. **Review follow-up** (FORK.md #103): the quit hook snapshotted the chain map one phase too early — `App::shutdown` collects the quit futures *before* `windows.clear()` + `flush_effects()`, and that flush is itself a persist site (`MultiWorkspace` release → `mark_closed` → `cold_close_solution` → `persist_all_rows`), so the last flush of the session landed in an already-emptied map and, with nothing else in flight, the future was Ready on first poll and the write got no background turn at all. `entries_persist_chain` is now a `PersistChains` newtype over `Rc<RefCell<…>>` and the `async move` body takes the map on its FIRST poll; an `AsyncApp::update` there is impossible because `shutdown` holds `&mut App` across the block, and that same borrow is what makes the deferred take sound. Also: the two `close_then_reopen_*` ordering tests were passing on the default seed even with `prev.await` removed, so they now run `iterations = 32` (mutation dies on both; green at `ITERATIONS=500`). |
| 2026-08-30 | complete | [`plans/2026-08-30-git-panel-commit-tab.md`](plans/2026-08-30-git-panel-commit-tab.md) — **phase 3**: the git panel's tabs become **`Changes \| Commit`**. History is deleted outright, and the git graph loses its inline right-hand commit-details sidebar — whose `min_w(px(300.))` was the only min-width in the view, and deleting it is what makes the graph viable in the compact utility half phase 2b moved it into. Its changed-files tree, message split, markdown style and client-side +/− fold were **relocated, not rebuilt**, into a new `crates/git_ui/src/git_panel/commit_tab.rs`, because `git_graph` depends on `git_ui` and never the reverse — the same dependency edge that makes graph→panel a **direct typed call** (deferred through `cx.defer_in`: `select_entry` is reachable from `invalidate_state` and the deserialize path, where a synchronous `workspace.update` double-leases) and panel→graph a **GPUI event** (`Event::CommitTabClosed(Vec<Oid>)`, whose payload is load-bearing — the event reaches every `GitGraph` in the window, so one graph's close must not deselect another's). Selecting a commit opens a closable Commit tab with the full message, a `short hash · author · date` row, whole-commit +/− totals and a changed-files tree; double-clicking a file opens that file's diff **for that commit** in the centre pane (`CommitView::open_file_diff`, reused verbatim); a multi-row selection renders "N commits selected". `show_commit_selection` takes a `CommitSelectionSource { UserGesture, Background }` because a background re-anchor after a `git fetch` landing in a terminal was yanking the panel off Changes while the user typed a commit message; a re-anchor that *fails* (`commit --amend`) closes the tab instead. Deleting History also opened a command-palette hole — the keymap context narrows but `.on_action` registrations do not — closed by unregistering 20 selection-scoped actions across 13 handlers while the Commit tab is active, plus `selected_file_history_target` returning `None` off Changes, because `git::FileHistory` is registered by `git_graph` on the *Workspace* element and so bypasses any guard living on the panel's own registrations. `6233753d7d` `509cf04ee8` `96524f2bf5` `85174e727e` `91cccb6592` `11b0a6af1f` `1a6699868f` `7f84d9617a` `6e37650b31` `3ec992a49f` `cff98212ff` `8d3b6acdaf` `62a6a0425f` `b4d7f666ef` `8093f946e0` `44312c93d8` `97032cfca0` `51c7889898` `193690aea6` `5856d346eb` `63ba1de90a` `15acd407ce` `7c010e3cf9` `561960506a`. **Task 6 (live verification + docs)** drove every state green against a real headless editor and fixed the three defects it found: the tab body's vertical budget (at a dock's height the message block pinned at its 200px cap plus a three-line identity row left the changed-files tree **zero** pixels — solved with `min_h` floors and flex, *not* a fraction of a bounds read, because a `cx.notify()` raised during a draw is discarded), the identity row itself (`short hash · author · date` as plain `Label`s with the email and time-of-day in a tooltip, replacing a markdown run that wrapped to three lines), and the lone "Changes" tab reading as a section header (`render_tab_bar` styled only the *inactive* branch — the active tab now carries a 2px `border_focused` underline, the solution band's project-tab idiom). Then a visibility sweep took `commit_tab` back to a private `mod` — Task 1 had made it `pub` with ten `pub` items purely so the now-deleted sidebar could import them, and `pub` inside a `pub mod` raises no `dead_code` warning, so it would never have surfaced on its own. FORK.md decision #100 (+ an amendment to #55: its "third changed-files tree" extraction trigger has now fired and is deliberately deferred). Final gates: **381 tests / 0 failed** (`git_ui` 320 + `git_graph` 61), `cargo fmt --all --check` clean, `./script/clippy` naming only the nine pre-existing findings in `panel_buttons.rs`, `git_panel.rs::build_commit_message_prompt`, `commit_view/affected_files.rs`, `stashes.rs` and `log_toolbar/branch_popover.rs`. |
| 2026-08-27 | complete | [`plans/2026-08-27-solution-band-utility-section.md`](plans/2026-08-27-solution-band-utility-section.md) — **phase 2b**: the band's utility half hosts **three** contents instead of one. `Workspace`'s single type-erased slot became a `HashMap<UtilityKind, AnyView>` (`UtilityKind` lives in `workspace`, which must not depend on the occupant crates); which kind a Solution shows is a persisted `BandState.utility_kind` (`utility_kind TEXT NOT NULL DEFAULT 'terminal'`, `CREATE TABLE` **and** idempotent `ALTER TABLE`); the opaque background moved onto the band's `half()` so occupants no longer each carry one; `GitGraphPanel` and `DebugPanel` both lost their `Panel` impls and are installed into the keyed slot from `zed.rs` (dropping the debugger's `Panel::position` is what makes its band orientation deterministic); a three-button group in the status bar switches content, hides the section when the active button is re-clicked, and sits **ahead of** the session tab strip so it survives clipping (it is the only path to the git graph); the project-zone panel toggles moved into the project toolbar and the two vertical dock strips were deleted — which also deleted `DOCK_STRIP_WIDTH`'s 40px correction term from `dock_resize_target_size`; and `ctrl-shift-a` toggles the band's dialog half, bound only in the `Workspace` context so it does not shadow `editor::SelectAll` in `Terminal` / macOS `Editor`. `134a51bc05` `c180820740` `850712d29d` `009aefddd3` `628ab25064` `8561f3b461` `d66a2fb9da` `57e30ff613` `1f72092462` `84dd8ef2a1` `3ceb6f433d` `be2265c6d4`. **Task 9 (live verification + docs)** drove all six checks green against a real headless editor and swept the cleanups the phase accumulated: `752490b760` (the pre-existing `test_action_namespaces` failure — `find_in_path` was missing from the expected set since `6217672762`), `18f9f338b0` (Windows had bound `ctrl-\`` to the never-loaded `terminal_panel::Toggle`, so it had no terminal toggle at all), `98962034c1` (stale dock-strip prose), `993ca8b42d` (the project toolbar drew its divider with nothing to its left when all three panel buttons were hidden), `f59bf5417b` (three rustfmt violations only `cargo fmt --all --check` caught), `ad1cb69b71` (FORK.md owner column). FORK.md decisions #95–#99. The final whole-branch review then found two Important cross-task defects: a pre-hydration no-op check that let startup hydration override a choice the user made during the DB-open window (`195152a012`, fixed uniformly across all four `set_band_*` setters), and a focus claim the fixer **disproved** live — the workspace's focus-lost listener already lands focus on the active pane — pinned with a test instead of new release logic (`92e7e27f93`). Final gates: **1459 tests / 0 failed**, `cargo fmt --all --check` clean, scoped `./script/clippy` naming only the seven pre-existing `git_ui` findings. |
| 2026-08-27 | complete | [`plans/2026-08-27-solution-band-height.md`](plans/2026-08-27-solution-band-height.md) — phase 2b, first slice: the **Solution band gets a height of its own**. Phase 2a shipped it content-driven (~128px, a ~30px transcript region), so the redesign's primary surface showed no conversation. `BandState` gains a fourth field `height` (absolute logical pixels, `MIN 140` / `DEFAULT 320` / `MAX 4096`) persisted in the same `solution_band_state` row through the same 400ms cancel-on-replace debounce as `divider_ratio` (`band_height REAL NOT NULL DEFAULT 320`, added to `CREATE TABLE` **and** via an idempotent `ALTER TABLE ADD COLUMN` for installs that already ran phase 2a); `SolutionBand::render` paints at `effective_band_height(stored, viewport)` — a pure render-time 80%-of-window cap that is never written back, because a `cx.notify()` raised during a draw is discarded; and a deferred full-width top-edge handle (`DraggedBandEdge`, `cursor_row_resize`, double-click resets to 320) commits from `on_drag_move`, not `on_drop` (FORK.md #84). `8c00d8845c` `30b3770ef5`. Live-verified headless at 1920x1080: drag, MIN clamp, 80% cap with the row left untouched, double-click reset, and the height surviving a real process restart. FORK.md decision #94. |
| 2026-08-26 | complete | [`plans/2026-08-26-solution-band-layout.md`](plans/2026-08-26-solution-band-layout.md) — phase 2a of 3: the **Solution band**. AI dialogs got their own full-width region between the project zone and the status bar, with the terminal beside them behind a draggable divider. `Workspace` gained two `AnyView` slots (`solution_band_item` + `solution_band_utility_item`, the `project_toolbar_item` idiom); chat left `ConsolePanel` entirely — its strip is now the status-bar `solution_agent::session_tab_strip` and its content is the band's dialog half, keyed on per-Solution `SolutionAgentStore::active_dialog_session`; `ConsolePanel` became terminal-only, undocked, and is rendered inside the band's utility section; the divider position, the utility section's visibility and the selected dialog persist per Solution in `solution_band_state`; the `solutions_status` ("`<Solution>` · N projects") and `SolutionAgentStatusItem` ("AI: N") status-bar indicators were deleted. `055cc800a8` `4bf7f3ab6b` `cdf5b247e8` `304488a18c` `f00afe7b93` `4f26fa0378` `54895eb2d7` `31305a49d7` `a15d753bb7` `3d98ab815b` `1e759b3a24` `a5954bcf86` `6c29b8b63f` `5b23ee5e16` `3861a2adfa` `e3eb066ce7` `2ccff0f3af`. FORK.md decisions #91–#93. Phase 2b (GitGraph + Debug into the utility section, delete the vertical dock strips, band height / horizontal handle) and phase 3 (git panel `Changes \| Commit`) were planned and shipped separately — see the rows above. |
| 2026-08-26 | complete | [`plans/2026-08-26-solution-scoped-sessions.md`](plans/2026-08-26-solution-scoped-sessions.md) — phase 1 of 3: remove the member-project binding from AI sessions. Chat tabs stop being filtered by the active member (`console_panel::tab_in_scope` treats every `ConsoleTab::Chat` as `Unscoped`); `create_session` and its `_with_cwd`/`_with_parent` variants stop taking/stamping `member_id`, `cwd: None` means the solution root; the `member_id` field is deleted from the model, DB read/write path and the project label; the startup migration clears (not backfills) `member_id` on upgrade; the member-less-solution chat/UI guards are removed (terminal gating kept). Spec: [`plans/2026-08-26-solution-band-ai-dialogs-design.md`](plans/2026-08-26-solution-band-ai-dialogs-design.md) (§1 "Scoping model"; phase 2 = solution-band layout, phase 3 = git panel Commit tab, both planned separately). FORK.md decision #89. |
| 2026-08-12 | complete | [`plans/2026-08-12-diff-gutter-and-connectors.md`](plans/2026-08-12-diff-gutter-and-connectors.md) — diff readability arc, seven operator items in four parallel workstreams. Per-hunk Stage/Restore hover buttons off by default; IntelliJ-style connector ribbons in a 36px strip between the two number columns (`crates/editor/src/split_connectors.rs`, hunks paired by zipping both panes' hunk lists, one filled path per ribbon because GPUI double-composites overlapping bounding boxes); gutter rework putting the breakpoint indicator on the line-number cell and runnables/bookmarks in an icon column, plus a gutter↔text border and `min_line_number_digits` 4 → 3; revision-aware `git blame <rev> -- <path>` with a `BlameBaseSource` side-channel so the split diff's detached base-text buffers can be annotated; and the soft-wrap root cause (a multibuffer took its `LanguageSettings` from the first excerpt, so one Markdown file wrapped the whole changeset) plus a wrap toggle in both diff toolbars. Measured: split-diff gutters 98/104 px → 72/72 px, centre 204 px → 182 px *with* ribbons; the normal editor kept its width and gained the IntelliJ arrangement. FORK.md decisions #58–#61. |
| 2026-07-13 | complete | [`superpowers/plans/2026-07-13-rename-1-identity.md`](superpowers/plans/2026-07-13-rename-1-identity.md) — identity model. Slug ids → surrogate counters `SolutionId(i64)` / `MemberId(i64)` / `CatalogId(i64)` (SQLite rowids; `name` / `root` / `local_path` become ordinary mutable columns, which is what makes rename cheap). Catalog entries became templates (`origin_catalog_id` is provenance only, a member owns its `name`); `solution_sessions.member_id` replaces cwd-inference for the project label + console-tab scoping; per-solution MCP socket dirs keyed by the numeric id, stale slug dirs swept at startup. Cross-DB migration (`solution_legacy_ids`) remaps `solution_agent.db`'s slugs — rehearsed against the operator's REAL databases: 16 solutions / 43 members / 14 active / 47 catalog in → same out; 176 sessions preserved, 169 remapped, 70 `member_id`s backfilled, 0 dangling `active_member` rows. `a81166f241` `b22518b815` `f573212060` `9c3e0e9d1e` `0a1407c8a3` `defd92e20b` `a1917ea87c` + `03ec37164d` (the socket injects `solution_id` as a JSON *number*, so every scoped tool's `String` param had to flip to `i64`). FORK.md decision #50. |
| 2026-07-13 | complete | [`superpowers/plans/2026-07-13-rename-2-folder-move.md`](superpowers/plans/2026-07-13-rename-2-folder-move.md) — rename moves the folder, in two halves. **Hot** (editor live): Unicode folder-name derivation (no transliteration), collision + same-filesystem check (cross-device = hard error), `rename(2)`, compat symlink old→new so in-flight *string* paths keep resolving, owning DB rows updated, `pending_path_migrations` row queued. **Cold** (`path_migrations::drain_and_apply` in `SolutionStore::init_with_db`, before any window opens): rewrites every path-bearing row across the app DB (`workspaces`, `editors`, `terminals`, `breakpoints`, `bookmarks`, `trusted_worktrees`, `toolchains`, `console_panel_state`, …) + the agent DB, moves/merges the claude transcript bucket, repairs git worktrees, removes the symlink, deletes the row — idempotent and crash-safe. `workspaces.paths` is rewritten **in place**, so the window keeps its `workspace_id` and its panes/tabs/docks. Verified live (`btest` → `Проект Тест`). New `solutions.rename_member` MCP tool + rename-member modal. `37763a1897` `5a3e92e403` `8012b06d8d` `107d47a40f` `27110814b5` `0a7843b2d4` `5a010d842d` `b60545b59b` `22e071d5ce` `eb3eb809f4` `5030f40efc` `5e2b6d661c`. FORK.md decision #52. |
| 2026-07-13 | complete | [`superpowers/plans/2026-07-13-rename-3-claude-settings-and-sockets.md`](superpowers/plans/2026-07-13-rename-3-claude-settings-and-sockets.md) — claude settings + sockets. Sockets / lock / upload spool moved `config/` → `state_dir()` (`~/.spk/sawe[-dev]/state/`) with a one-time sweep of the old location. The editor owns a claude settings layer passed as `--settings <file>` (`<state>/solutions/<id>/claude-settings.json`): `WorktreeCreate` / `WorktreeRemove` hooks point at the running binary itself (`sawe --worktree-hook`), so agent worktrees land in `<solution_root>/.agents/worktrees/` instead of `<member>/.claude/worktrees/`, and `autoMemoryDirectory` → `<solution_root>/.agents/memory` — verified with a real claude subagent. `solution_id` became optional in the solution-scoped MCP tools: the per-solution socket injects it and overrides any foreign id; on the global socket, omitting it is a clear error, never a silent wrong-solution success. `47c38dbca7` `4573ee11b3` `307eea9bde` `710361bb66` `8a60552b22` `7ad284513f` `8b21dc3fdd` `69a83c8918` `16d9f9295d` `399eb2e471` `781b484308` `48a5b7b04c` `6ec94dbcb1`. FORK.md decision #51. |
| 2026-07-10 | complete | [`plans/2026-07-10-god-object-refactor-tier4.md`](plans/2026-07-10-god-object-refactor-tier4.md) — Tier 4 (audit-driven): Stage A logical splits (`store/tests.rs` 13584→9+tree, `supervisor.rs` 1323→23 3-way, `model.rs` tests out, `solutions/mcp` deeper, `modals.rs` 1055→97, `solutions/store.rs` 1602→948) + Stage B `store.rs` cluster relocations (reconciler/hydration/teardown/acp_event → `store/*.rs`): **`store.rs` 7998→3559, −55%**. All verbatim, 563/146/34 green, whole-binary clean. |
| 2026-07-10 | complete | [`plans/2026-07-10-god-object-refactor-tier3.md`](plans/2026-07-10-god-object-refactor-tier3.md) — Tier 3: relocated the ~36-method supervisor/judge/auditor cluster out of `store.rs` into `store/supervisor_engine.rs` via the partial-class idiom (`store.rs` **10161→7998, −2163**; new file 2183). Verbatim (per-method diff = 0), 563 tests green after every commit, whole-binary clean. Design decision (FORK.md #49): trait-seam dependency-inversion is NO-GO (GPUI double-borrow + `WeakEntity<Store>` async continuations + Store-only `cx.emit`); partial-class source relocation is the real win. |
| 2026-07-10 | complete | [`plans/2026-07-10-god-object-refactor-tier2.md`](plans/2026-07-10-god-object-refactor-tier2.md) — Tier 2 structural seams: `session_view.rs` satellites (3044→1864, `1dafbc0bcf`) + `store.rs` `ModelCatalog` (`ceb233f8e7`). Finding: `store.rs` is a genuine coordinator, not flat bloat — field extraction yields marginal shrink (−9 lines), so PoolManager/ArchiveGc/TeammateWatchers deferred; real reduction needs Tier-3 trait-seam work. |
| 2026-07-10 | complete | [`plans/2026-07-10-god-object-refactor-tier1.md`](plans/2026-07-10-god-object-refactor-tier1.md) — Tier 1 mechanical split of 4 god-object files (`solutions/mcp.rs` 5766→28, `solution_agent/{mcp 8483→52, db 2896→485, conversation_render 2246→719}.rs`) into submodule trees. Pure relocation, one commit per file (`b18a9292f7`/`aa9756f91e`/`0fb74532bc`/`ff9ae0c02d`). Whole-binary build clean; 146 + 563 tests green. Tier 2/3 (session_view, store.rs sub-objects, SupervisorEngine) deferred. |
| 2026-05-15 | complete | [`plans/2026-05-15-picker-and-panel-ui-tweaks.md`](plans/2026-05-15-picker-and-panel-ui-tweaks.md) — Picker dropdown polish + Project panel header. Screenshot: [`plans/2026-05-15-picker-and-panel-ui-tweaks-screenshot.png`](plans/2026-05-15-picker-and-panel-ui-tweaks-screenshot.png). |
| 2026-05-15 | scoping | [`plans/2026-05-15-remote-control.md`](plans/2026-05-15-remote-control.md) — Remote Control panel + Android client (multi-phase arc R-1 through R-6). R-1, R-1.5, R-2, R-3, R-4 shipped; R-5 lives in sibling repo `sawe-mobile` (decomposed into R-5a..R-5d). |
| 2026-05-16 | complete | [`plans/2026-05-16-remote-control-R5a-android-bootstrap.md`](plans/2026-05-16-remote-control-R5a-android-bootstrap.md) — R-5a: bootstrap `sawe-mobile` repo + two-module Gradle layout (`:core` JVM connection lib, 30 green tests + `:app` Android Compose Material 3 surface). Sibling-repo commits `77eb966` (bootstrap) → `4e478f1` (`:cli` smoke client + six-step `LiveEditorIntegrationTest`) → `d83ab47` (`:core` `api` promotion after Android SDK install surfaced `:app:compileDebugKotlin` failure). `:app:assembleDebug` now produces a real 9.5 MB APK; `:core:test` 30 PASSED. WS+TLS+HMAC + JSON-RPC client wired end-to-end. |
| 2026-05-16 | complete | [`plans/2026-05-16-remote-control-R5b-qr-scanner.md`](plans/2026-05-16-remote-control-R5b-qr-scanner.md) — R-5b: zxing-android-embedded QR scanner replaces the R-5a paste-URL stub. Sibling-repo commit `6e444e5`. APK 10.9 MB. Gotcha: zxing-android-embedded needs an explicit `androidx.appcompat` dep — its AAR registers `CaptureActivity` without bringing the AppCompat style classpath. |
| 2026-05-16 | complete | [`plans/2026-05-16-remote-control-R5c-solutions-sessions-list.md`](plans/2026-05-16-remote-control-R5c-solutions-sessions-list.md) — R-5c: solutions list → solution detail (with sessions list + live `agent_session_state_changed` re-fetch) → session-detail stub. Navigation Compose 2.8.4. Sibling commit `7fa4615`. `:core` tests 30 → 41, APK 11.18 MB. Gotcha: Kotlin smart-cast doesn't fire across module boundaries on `JsonRpcResponse.error` — lift to local val before null-check. |
| 2026-05-16 | complete | [`plans/2026-05-16-remote-control-R5d-chat-streaming.md`](plans/2026-05-16-remote-control-R5d-chat-streaming.md) — R-5d: chat surface (bubbles by role, optimistic user message, Send / Cancel / state-dependent compose row, id-only `agent_session_message_appended` triggers `get_session` refetch). **Closes the R-5 arc** — sibling commit `6ef0cd7`. APK 11.22 MB, `:core` tests 41 → 45. Known limits → filed as R-5e (server-side enrichment of `EntrySummary` + notification payloads). |
| 2026-05-16 | complete | [`plans/2026-05-16-remote-control-R5e-entry-enrichment.md`](plans/2026-05-16-remote-control-R5e-entry-enrichment.md) — R-5e: server-side enrichment shipped on `main` as `d8592b05dc`. `EntrySummary` gains optional `markdown` / `images` / `tool_call` / `plan` fields gated behind `include_full_content` + `include_images` params; `agent_session_message_appended` notification carries `entry_index` + `role` + `preview`; new `remote.solution_agent.get_session_entry` tool. `solution_agent` tests 83 → 90, R-4 proxy_e2e still green, allow-list extended. Wire sizes documented (preview 1.77 KB / full 6.32 KB / full+images 46.5 KB on a 9-entry synthetic). |
| 2026-05-17 | complete | [`plans/2026-05-17-remote-control-R5f-client-rich-rendering.md`](plans/2026-05-17-remote-control-R5f-client-rich-rendering.md) — R-5f: Android client now consumes R-5e's enriched EntrySummary (markdown / images / tool_call / plan). Diff streaming via `get_session_entry(entry_index)` driven by `agent_session_message_appended` payload — bandwidth proportional to new content per turn instead of full history per token batch. Sibling commit `ee804aa`. `:core` tests 45 → 54. APK 11.58 MB. `multiplatform-markdown-renderer-m3:0.27.0` for rich render; Coil dropped because the lib's `ImageTransformer` takes a pre-decoded `Painter` synchronously. |
| 2026-05-17 | complete | R-5g create-session — **server**: new `solution_agent.list_agents` MCP tool + allow-list extension (sawe commit `3fb5ee51ac`, solution_agent tests 90 → 91); **client**: FAB stub replaced with Material 3 AlertDialog (RadioButton agent picker + optional initial message + one-shot auto-open-and-retry on `no_active_workspace_for_solution` error) (sibling commit `41531a1`, `:core` tests 54 → 57, APK 11.59 MB). No plan-doc (small focused phase shipped inline). |
| 2026-05-17 | complete | R-6a network resilience — WS reconnect with 1-2-4-8-16-30s exponential backoff, OkHttp `pingInterval(30s)` NAT keep-alive, `queueCall` for `send_message` with 5-min TTL + FIFO reflush on Connected transition, subscription auto-restore, ConnectionState StateFlow + Compose banner (tertiary "Reconnecting…" / error "Re-pair required"). MainViewModel: `lastSeenEntryIndex` per session + refresh on every Disconnected→Connected. New `RemoteTransport` seam (`OkHttpRemoteTransport` + `FakeRemoteTransport` for tests). Sibling commit `c69e7e3`, `:core` tests 57 → 72, APK 11.59 → 11.63 MB. No plan-doc (inline scope). |
| 2026-05-17 | complete | R-6b production polish — Pairing persistence via AndroidX `EncryptedSharedPreferences` (cold-start skips QR if pairing exists) + Settings screen (server info / fingerprint / Forget Server / Re-pair / About) + adaptive launcher icon + INTERNET permission + signed-release Gradle config (gates on `SPK_RELEASE_*` properties; keystore-free `assembleRelease` succeeds for R8 verification) + ProGuard rules for kotlinx.serialization + expanded README. Sibling commit `c517f03`. `:core` tests stable at 72. Debug APK 11.63 → 12.0 MB (+security-crypto); **release APK 2.12 MB after R8** (5.4× shrink). No plan-doc (inline scope). |
| 2026-05-17 | complete | [`plans/2026-05-17-remote-control-R6d-android-disk-persistence.md`](plans/2026-05-17-remote-control-R6d-android-disk-persistence.md) — R-6d: Disk persistence. `QueueStore` interface in `:core` + `EncryptedQueueStore` impl in `:app` (24h TTL, FIFO, survives force-kill). `DraftRepository` (debounced 500ms saves + read-and-clear bounced channel). `LastSeenRepository` per session. `NavStateRepository` with route-template resolver. Single shared `AppMasterKey`. Sibling commit `ff9ca8c`. `:core` tests 72 → 82 (+6 QueueStoreTest + 4 RemoteClientLifecycleTest). Release APK 2.12 → 2.22 MB. |
| 2026-05-17 | complete | [`plans/2026-05-17-remote-control-R6e-pagination-incremental-resume.md`](plans/2026-05-17-remote-control-R6e-pagination-incremental-resume.md) — R-6e: cursor-style pagination on `get_session` (before_index/after_index/count + total_count + always-populated EntrySummary.index). `list_sessions` pagination too. Server commit `b756392b52` (solution_agent 91 → 99 tests, R-4 proxy_e2e still green, FORK.md updated). Client commit `c7fbddc` (`:core` 82 → 87 tests, paginated initial pull count=50, LazyColumn auto-load-older via lastVisibleItemsInfo, resumeSession via after_index=lastSeen with gap-detect safety net). Release APK 2.22 → 2.24 MB. |
| 2026-05-17 | cancelled (upstream gap) | [`plans/2026-05-17-remote-control-R6f-ws-compression.md`](plans/2026-05-17-remote-control-R6f-ws-compression.md) — R-6f: WS `permessage-deflate`. **Punted: upstream `tokio-tungstenite 0.28`/`0.29` and `tungstenite 0.28`/`0.29` do not implement the extension** (no Cargo feature, no `WebSocketConfig` knob, no API). Verified directly from cached crate sources. See [`findings/2026-05-17-remote-control-r6f-upstream-gap.md`](findings/2026-05-17-remote-control-r6f-upstream-gap.md). Listener unchanged. Re-open when upstream lands compression or we migrate the WS stack. |
| 2026-05-17 | complete | [`plans/2026-05-17-remote-control-R6c-multi-server.md`](plans/2026-05-17-remote-control-R6c-multi-server.md) — R-6c-multi: PairingRepository → keyed map (PairedServer), Servers list screen as nav entry when 2+ paired, R-6b → R-6c v1→v2 migration, per-server scoping on Draft/Queue/LastSeen/Nav repositories via `activeServerProvider` closure, queue blob lazy-migrates legacy `queued_messages_v1`, full ViewModel teardown on `switchToServer` (incl. lastSeenEntryIndex/connection banner/optimistic entries). Compose-Nav `startDestination` set once from `coldStartLandingRoute`. Sibling commit `d753b50`. APK 2.24 MB unchanged (no new deps). |
| 2026-05-17 | complete | R-6g local crash logging — `SpkApplication.onCreate` installs `Thread.setDefaultUncaughtExceptionHandler`; `CrashLogger` object writes timestamped stack traces with device/version metadata to `filesDir/crash-logs/`; rotates to keep 10 newest. Settings → "Diagnostics" entry with file count; tap opens `CrashLogsScreen` with view-dialog + Share-intent + Clear-all. App-private storage, no external uploads. `Icons.Filled.Warning` (BugReport not in core). Sibling commit `26c7151`. APK 2.25 MB. No plan-doc (inline scope). |
| 2026-05-19 | complete | [`plans/2026-05-19-chunked-upload-binary-frames.md`](plans/2026-05-19-chunked-upload-binary-frames.md) — chunked upload via WS binary frames + start-on-pick + resume. Mobile attachments no longer base64-inline over the network; upload starts when user picks file, runs in background with per-chunk progress, resumes after WS disconnect via `upload_status`, server-side `send_message_blocks` resolves `spk-upload://<id>` ResourceLinks → `acp::ContentBlock::Image`. Server: `65fd88d508` (UploadManager + 4 MCP tools + ResourceLink resolver), `30fe8eed6a` (`remote_control::BinaryFrameHandler` indirection — avoids feature-unifying second rustls CryptoProvider; WS cap dial-back 32 MiB → 1 MiB; allow-list extension). Mobile: `c270abb` (`UploadProtocol.kt` + `buildUploadChunkFrame` + 17 tests + `RemoteClient.sendBinary`), `47b719e` (`UploadManager` + `InFlightUploadsRepository` + ack-channel wiring), `8e5f10d` (compose-row start-on-pick + progress overlay + ResourceLink emission; `encodeAttachment` deleted). `:core` 222 → 233; server tests 132 + 38 = 170. |
| 2026-05-19 | complete | (no plan-doc — focused fix) `solution_agent` auto-wakes Cold sessions on MCP `send_message_blocks` via headless `Project::local(...)` (eval_cli pattern). `76bbd57912` (`make_headless_project_for_solution` helper — ACP adapter only reads `context_server_store` + `is_local`, no Workspace needed) + `c16b3017dc` (auto-wake in `store::queue::send_message_blocks` — detects cold session, builds headless project, calls `resume_session`, retries send). Mobile users can now message sleeping sessions and they wake silently. |
| 2026-05-19 | complete | (no plan-doc — focused fixes) Four mobile bug fixes shipped this morning: `6ed7870`-equivalent in mobile reverted insets+bubble batch; `0e088c0` (explicit Compose insets — lift compose bar above IME, clear system nav for targetSdk 36 implicit edge-to-edge; bundled `<thinking>` strip + `markdownPadding(block = 6.dp)` for the bubble-indent fix); `42fa4db832` (interim WS cap 64 KiB → 32 MiB, later dialled back to 1 MiB in `30fe8eed6a` once chunked upload landed); `f903109` (AtomicLong counter for `client_send_id` — eliminates same-ms collision risk). |
| 2026-05-19 | complete | (no plan-doc — focused fix) ID-based optimistic-bubble dedupe replacing the fragile `(role, preview)` content-match (closes the duplicate-bubble bug reported 2026-05-18 for long messages). Server commit `a9059b8566`: `acp_thread::SPK_CLIENT_SEND_ID_META_KEY` + `client_send_id_from_user_message` helper; `EntrySummary.client_send_id: Option<i64>`; `agent_session_message_appended` notification carries the id at top level for user-role appends. Mobile commits `1b82440` + `95a8630`: per-variant `ContentBlockDto._meta`, `stampClientSendId` encoder helper, `reconcileOptimistic` (id-match primary, content-match fallback), `SessionDetailStore.optimisticBlocksFlags` hack outright deleted (replaced with `optimisticClientSendIds: List<Long?>`), notification fast-pop by csid. Id rides on existing `acp::ContentBlock._meta` so `AcpThread::send` + `UserMessage` struct stay untouched. Mobile `:core` 202 → 222. |
| 2026-05-19 | complete | [`plans/2026-05-19-mobile-attachments.md`](plans/2026-05-19-mobile-attachments.md) — file + image attachments in mobile chat compose. Server-side new MCP tool `solution_agent.send_message_blocks` wrapping the existing `store::send_message_blocks` (which already takes `Vec<acp::ContentBlock>`) + allow-list extension (sawe `5adf824ef7`). Mobile: `:core` `ContentBlockDto` sealed type with `@JsonClassDiscriminator("type")` matching ACP's serde tag + pure-fn `encodeAttachment` routing image/* → Image block / text-like → Text block (4-backtick fenced) / binary → Failure chip (`817fa4a`). `:app` compose row gains attach button + PickMultipleVisualMedia + OpenDocument launchers + 5MB cap + preview LazyRow + `MainViewModel.sendMessageBlocks` (`e5e111a`). Queue persistence needs no schema bump (`QueuedMessage.params` already opaque); `parseExpiredSendMessage` extended for `send_message_blocks` bounce-recovery (`0f5ffe4`). Optimistic dedupe via parallel `optimisticBlocksFlags` (notification lacks timestamp, so `localSendTimeMs` abandoned). `:core` 185 → 202. V1 limits: ≤5MB; binary files refused with inline chip; image/file blocks dropped on TTL-bounce. |
| 2026-05-19 | complete | [`plans/2026-05-19-mobile-history-disk-cache.md`](plans/2026-05-19-mobile-history-disk-cache.md) — per-session chat-history disk cache on mobile. SessionHistoryRepository (per-server EncryptedSharedPreferences via prefixed keys, mirroring existing repo precedent) + SessionHistoryMerge pure-fn in `:core` (MergeOutcome FullReplace/Appended/GapDetected). SessionDetailStore.openSession loads cached entries instantly then asks server for the diff via `get_session(after_index=cache.lastIndex)`. Eviction wired on close_session / restart_agent / start_compact two-phase via pendingCompactSourceIds + onChildSessionCreated / GC sweep on refreshSessions / removeServer evictAll. Base64-blob strip (>4KB) keeps inline images out of the prefs file. Mobile commits `969b804` + `f7dbefe`. `:core` 179 → 185 (+6 merge tests). |
| 2026-05-19 | complete | (no plan-doc — small inline phase chain across 2026-05-19) Mobile UX additions piggy-backing on the server prerequisites shipped 2026-05-18: drop em-dash separator (mobile `6ed7870`); per-row delete on session list (mobile `fa998fe`); FAB create + per-row delete on solutions list (mobile `cb4d4ef`); chat overflow menu with Reset+Compact context wired to new MCP tools (mobile `36248bb`); context-fill meter in chat top bar (mobile `94bf4a0`); per-tool elapsed "Xs" badge (mobile `0783a40`); foreground-resume refresh fix for the stuck-Running pill (mobile `ee67318` + finding `2026-05-18-mobile-session-state-stuck-running.md`); session-Running elapsed badge in StatePill (mobile `71bb4a7` + server `516fcf0888` for `state_started_at_ms` on SessionSummary). Build-unblocker `c8ce1b1` (BOM 2026.04.00→.01 retraction, material-icons-extended explicit, markdownColor.codeText drop). Server-side allow-list extension for the new mobile-driven mutations: sawe `6c1536d1f1`. |
| 2026-05-18 | complete | [`plans/2026-05-18-per-tool-elapsed-timer.md`](plans/2026-05-18-per-tool-elapsed-timer.md) — per-tool elapsed "Xs" badge next to InProgress tool calls on desktop, plus the wire-side `tool_status_started_at_ms` so the mobile client picks it up later (task #9). 3 commits: `198c04de94` (acp_thread first-touch — adds `ToolCall.status_started_at: Option<DateTime<Utc>>`, monotonic stamp on Pending→InProgress), `ed689dba9f` (MCP `ToolCallSummary.tool_status_started_at_ms`), `53c5bc193d` (`conversation_render` badge + `session_view::ensure_tool_tick` mirroring `status_row::ensure_thinking_tick`; `status_row::format_elapsed` promoted to `pub(crate)` so both surfaces share `Xs / 1m02s / 1h05m` format). 169/169 tests green. FORK.md row added for the acp_thread first-touch. |
| 2026-05-18 | complete | (no plan-doc — small inline phase) `solution_agent` exposes `SessionSummary.max_tokens` + new `solution_agent.start_compact` MCP tool. Commit `ea1e0f7a67`. Unblocks mobile tasks #5/#6 once that repo's parallel WIP lands. |
| 2026-05-17 | complete | [`plans/2026-05-17-F-subagent-indication.md`](plans/2026-05-17-F-subagent-indication.md) — F: sub-agent indication UI shipped in three parts. **F-server** (sawe `104881302c`): `SolutionSession.parent_session_id`, `create_session` parent param with same-solution validation, `SessionSummary` gains `total_tokens` + `parent_session_id`, new `solution_agent.get_session_children` tool, `agent_session_created` payload carries `parent_session_id`, sqlite migration via idempotent `apply_idempotent_add_column`, allow-list extended. `solution_agent` tests 99 → 106. **F-desktop** (sawe `cd8a6aebb5`): `session_view/subagent_strip.rs` renders a DFS bubble strip above the status row; click routes through existing `SolutionSessionsNavigator::open_session`; `IconName::Circle` (tinted = current, muted = hollow); auto-hidden when top-level + no children. `solution_agent` tests 106 → 112. **F-phone** (sibling `1af444b`): DTOs extended; `AssistChip` LazyRow above compose row with state icon + truncated title + abbreviated token count; "Parent: …" chip when current is a child. `:core` tests 87 → 91, release APK 2.16 MB. Trade-off explicit: Claude Code's internal Task tool dispatches still won't surface — parent agents must call `create_session({parent_session_id})` for visibility. |
| 2026-05-16 | complete | [`plans/2026-05-16-remote-control-R4.md`](plans/2026-05-16-remote-control-R4.md) — R-4: `remote.*` proxy to the embedded MCP Unix socket (per-WS `UnixMcpProxy`, allow-list, agent_session_* notification fan-out). |
| 2026-05-15 | complete | [`plans/2026-05-15-remote-control-R2.md`](plans/2026-05-15-remote-control-R2.md) — R-2: server listener + TLS 1.3 + HMAC challenge handshake + MinimalDispatcher; FS-watcher reconciles listener state. |
| 2026-05-15 | complete | [`plans/2026-05-15-remote-control-R1-5.md`](plans/2026-05-15-remote-control-R1-5.md) — R-1.5: QR popover rendering replacing the R-1 toast stub |
| 2026-05-15 | complete | [`plans/2026-05-15-remote-control-R1.md`](plans/2026-05-15-remote-control-R1.md) — R-1: settings + status-bar widget + modal panel UI. Screenshot: [`plans/2026-05-15-remote-control-R1-screenshot.png`](plans/2026-05-15-remote-control-R1-screenshot.png). |
| 2026-05-15 | complete | [`plans/2026-05-15-clickable-tree.md`](plans/2026-05-15-clickable-tree.md) — hitbox-based clickable enumeration + click-by-id MCP tool (phase 1 + 1b labels) |
| 2026-05-15 | complete | [`plans/2026-05-15-headless-platform-real.md`](plans/2026-05-15-headless-platform-real.md) — native headless GPUI platform (no Xvfb). Screenshot: [`plans/2026-05-15-headless-platform-real-screenshot.png`](plans/2026-05-15-headless-platform-real-screenshot.png). |

---

## superpowers/ — personal local drafts (gitignored)

The folder `docs/superpowers/{plans,specs}/` is **gitignored** (.gitignore
entry: "Personal agent plans / specs (kept locally, not committed)"). Use
it for in-progress ideas before they're polished into a committed
`docs/plans/` or `docs/specs/` entry.

Pre-existing local drafts (not visible to a fresh clone or worktree):
- `docs/superpowers/plans/2026-05-{06,07,12}-*.md` — earlier rebrand work
- `docs/superpowers/specs/2026-05-*-design.md` — earlier design notes

Promote to `docs/plans/` (or `docs/specs/`) when ready to commit + dispatch.

---

## findings/ — discovery notes

Short, dated, single-fact notes from sessions: "ran a benchmark and got X",
"found a crate Y", "noticed library Z behaves W in case V". Filename
`YYYY-MM-<slug>.md`. 10–50 lines, no fluff.

| Date | Status | Topic |
|---|---|---|
| 2026-08-31 | handoff | [`findings/2026-08-31-session-handoff.md`](findings/2026-08-31-session-handoff.md) — **READ FIRST on resume.** Supersedes the 2026-08-30 handoff for everything after `df7b4165a5`; the sandwich redesign's three phases are untouched and still finished. **No UI shipped** — a correctness-and-cost session in `solution_agent` and `project`, driven off the previous handoff's "thin, none urgent" pool, which was hiding four real defects and a 19x performance win. **`/clear` and `/compact` never cleared the legacy blob**, so a migrated then wiped session had zero rows permanently and **four** reconstruction paths replayed the pre-wipe transcript — the user wiped a conversation, reopened the tab, and it was back (FORK.md #105). **`rebuild_streams` deep-copied the whole transcript at up to 62.5 Hz on the foreground**: measured 2.676 ms per rebuild on a 1,520-entry / 5.45 MB transcript, ~167 ms of foreground CPU per second of streaming, now 0.137 ms via `Arc` sharing (FORK.md #108). `get_session`/`get_session_changes`/`get_session_entry` now serve closed sessions and share one stream-local index space with `agent_session_message_appended`, with a cold-read cache validated per call against a database head (#105, #107). A quit could never shut down a *starting* language server and burned the whole 200 ms budget every other observer shares (#103). **49 unit tests were compiled, green and never run** — `[lib] test = false` in `project`/`worktree`/`fs`; 327 -> 376. FORK.md #55's tree-extraction trigger is **withdrawn permanently** (the "third tree" is a flat grouper, and #55's stated blocker was render-side), #97 gained a permanent ruling, and the MCP-sockets entry was renumbered **106** because it duplicated 17. Carries the five things not to re-derive — "a closed session has no writer" is **false**, `(session_id, change_seq)` as a cache key ships stale data, "no rows + epoch > 0" is now load-bearing so nothing may let the epoch outrun its rows, `Arc::get_mut` is the new trap while `make_mut` is not, and **a plan doc's checked-off box is not evidence about current client behaviour** (a Critical was rated off three stale plan docs; the client's `getSessionEntry` has zero call sites). Process: **eight mutation-caught gaps**, all "the test looked like it covered the case and did not", two found only by mutating the *test hooks*; and four defects that were comments stating something false — one documenting the wrong function, inside the round fixing that class. **A restore could rewrite a transcript it had never read, four ways** — `migrating` authorises a destructive rewrite and was derived from "the entry set came back empty", which is where every way of *failing* to read one lands: one transient sqlite read error deleted a 3-row transcript unrecoverably, and a failed *epoch* read did the mirror image, repainting a genuinely `/clear`ed conversation and rewinding the epoch permanently (FORK.md #110). Also shipped: `tooling/test_target_guard`, a build script that fails the workspace check on that `[lib] test = false` combination (FORK.md #109 — CI here is doubly disabled and `cargo test --workspace` is never run, while `cargo check --workspace` runs continuously under flycheck); a property test for `rebuild_streams`'s decoration, where **three behaviours had zero coverage anywhere in the repo**; and the ten missing `LICENSE-GPL` symlinks that had made `script/check-licenses` fail on every run. Gotchas: `git commit <path>` commits the working tree, so two agents dirty in one file means the second sweeps the first; do not build a push range from a stale `git log` (one unreviewed commit reached origin that way); and `| tail` masked an exit code for two different agents in opposite directions. |
| 2026-08-30 | handoff | [`findings/2026-08-30-session-handoff.md`](findings/2026-08-30-session-handoff.md) — **READ FIRST on resume.** Supersedes the 2026-08-27 handoff for everything after `8bed974075`. **Phase 3 is COMPLETE, so all three phases of the sandwich redesign are finished**: the git panel's tabs are exactly `Changes | Commit`, History is deleted, and the git graph lost the inline commit-details sidebar whose `min_w(px(300.))` was the only min-width in the view ([`plans/2026-08-30-git-panel-commit-tab.md`](plans/2026-08-30-git-panel-commit-tab.md), FORK.md #100). Three backlog items also cleared: **`ctrl-\`` now leaves the caret in the band's active terminal** (and closing the focused tab re-homes focus inside the console instead of ejecting to the editor); **a soft close no longer discards in-flight transcript writes** — a silent, permanent data loss on every tab and window close ([`plans/2026-08-30-entries-persist-chain-teardown.md`](plans/2026-08-30-entries-persist-chain-teardown.md), FORK.md #101); and an entry-row flush is now **one batched write** (200 rows: 407 executor turns → 7, 38 observable partial row sets → 0, ~200× less fsync traffic). Carries the four things not to re-derive — the `git_graph → git_ui` edge that dictates phase 3's whole architecture, the command-palette hole that deleting History opened and the one cross-crate reader (`git::FileHistory`) that escaped the fix, why a drained persist chain is **retained under its key rather than detached** (detaching shipped a Critical: a close→reopen deleted the reopened session's new message, 4 rows vs 3), and that the legacy row layout is *intended* behaviour rather than the hazard that blocked that item for two sessions. Plus this session's gotchas: harness diagnostics wrong a dozen-plus times including a full wall of phantom `E0061`s, mutation-test in a worktree not the shared checkout, and `script/run-mcp` only compiles a *missing* binary (three agents launched a stale one). |
| 2026-08-27 | handoff | [`findings/2026-08-27-session-handoff-2.md`](findings/2026-08-27-session-handoff-2.md) — **READ FIRST on resume.** Second session of 2026-08-27; supersedes the handoff below for everything after `c14a18328c`. **The band now has a height of its own** (plan complete, `8c00d8845c`…`e2396c2cab`) **and its utility half hosts three contents** (phase 2b, tasks 1-6 of 9, `134a51bc05`…`57e30ff613`): the keyed `HashMap<UtilityKind, AnyView>` slot, the persisted `utility_kind`, the git graph and the debugger both de-docked into the band, and a three-button group in the status bar that switches and hides the section. **Phase 2b is COMPLETE through `92e7e27f93` — all nine tasks, the final whole-branch review and its fix wave. Phase 2 is finished; the next unstarted phase is 3, the git panel (spec §5).** **The one thing to read:** the band's three-part layout invariant (`min_h_0()` on the workspace column, `flex_none()` on the status bar, a shrinkable band) — removing any one silently pushes the status bar off a short window; measured at 1280x384 as **0 visible status-bar pixels** before the fix. `BAND_RESERVED_HEIGHT` is a coarse guard, NOT what keeps it on screen. Also: `windows.resize` is a new debug-only MCP tool (it fans out into `save_window_bounds`, so a release-build agent resize would persist into the real workspace DB). **Next:** phase 2b tasks 7 (relocate the project-zone toggles, then delete the vertical dock strips — order is load-bearing), 8 (`ctrl-shift-a` — the binding collides in `Terminal`/macOS `Editor` contexts and nothing remembers the last-selected session), 9 (live verification + docs + the pre-existing `test_action_namespaces` failure). Carries every ruling, the pool and the gotchas — including that the machine hit **100% disk** mid-session and that a screenshot renders the retained scene without drawing. |
| 2026-08-27 | handoff | [`findings/2026-08-27-session-handoff.md`](findings/2026-08-27-session-handoff.md) — **READ FIRST on resume.** **Phase 2a of the Solution band is COMPLETE and pushed through `de19ba80af`.** Shipped T7 (draggable divider + per-Solution persisted band geometry in the agent DB, `3d98ab815b`+`1e759b3a24`), T7b (cold-hydrated sessions now reach `by_solution`, so the status-bar session tab strip survives a restart, `a5954bcf86`…`5b23ee5e16`), T8 (status-bar indicators deleted, sweep, FORK.md #91-#93 correction pass, `3861a2adfa`…`8e6f1c4db2`), then a whole-branch review + its 8-commit fix wave (`95a4d5523a`…`a5d74aafbb`) and two tail commits. 1175 tests, fmt clean, scoped clippy green. **The one thing to read:** fixing the empty strip woke `gc_orphan_members`, a destructive GC that had been a silent no-op — against the maintainer's real DB it would have irreversibly hard-purged **~18 session transcripts** on the next open of `Sawe1`. Now gated on a live `acp_thread`; cold orphans log at `target: "solution_agent::gc"`. **That backlog still exists in the database and is a decision waiting for the maintainer** (`close_session` is the reversible way to retire one). **Next:** phase 2b. (The pool's old item 1 — the pre-existing `RunController::run` double-lease crash — was **fixed 2026-08-27** in `4038606620`; it needed two halves, not just the `&mut Workspace` threading that item prescribed. Do not re-implement it.) Plan phase 2b (GitGraph + Debug into the band's utility section, delete the vertical dock strips — `DebugPanel` is the most coupled; the band still has no height of its own, ~128px), then phase 3. Carries every ruling, the deferred backlog (incl. the `entries_persist_chain` wholesale cancellation, whose naive fix is dangerous) and the session's gotchas — `mcp__sawe__*` drives the LIVE editor, `run-mcp --runtime-dir` does not isolate the socket, harness diagnostics stale 10×, unscoped clippy RED on pre-existing `denoise`/`git_ui` debt. |
| 2026-08-26 | handoff | [`findings/2026-08-26-session-handoff.md`](findings/2026-08-26-session-handoff.md) — **Superseded by the 2026-08-27 handoff above.** Designed a 3-phase UI redesign (spec `plans/2026-08-26-solution-band-ai-dialogs-design.md`) and shipped **phase 1 whole**: AI sessions are now purely Solution-scoped — no `member_id`, cwd at the solution root, tabs never filtered by the active project, migration clears existing bindings, member-less-solution guards gone (`8f4350f267`…`761bac3c49`, FORK.md #89). Also shipped **six supervisor fixes** off a user bug report, including a **Critical live on `main`**: a supervisor the user switched off could turn itself back on (`d4e668375b`), plus month-day reset parsing, clear-on-successful-turn, store-wide sibling propagation, dated ETAs, and a narrowed un-park trigger (`2cc8d97155`…`84efa8ba78`); and branch protection no longer protects by branch *name* out of the box (`be570d79e9`, FORK.md #90). Started **phase 2a (the solution band)** and got 6 of its 8 tasks in: the band renders dialog + terminal side by side, session tabs live in the status bar, `ConsolePanel` is undocked (`4bf7f3ab6b`…`a15d753bb7`). **Phase 2a was finished and pushed the next session — see the 2026-08-27 handoff above, which supersedes the resume instructions below.** (That ledger is gitignored and has since been deleted; its durable content lives in the 2026-08-27 handoff.) Carries the session's active gotchas — `mcp__sawe__*` drives the LIVE editor, harness diagnostics are stale ~8×, GPUI double-lease, `ui::ContextMenu` won't nest. |
| 2026-08-26 | **fixed 2026-08-27** | [`findings/2026-08-26-run-controller-terminal-double-lease-crash.md`](findings/2026-08-26-run-controller-terminal-double-lease-crash.md) — **Run (or `run_config.run` over MCP) on any Terminal-type run config double-lease-panicked the whole editor.** `RunController::run` always runs nested inside an active `Workspace::update` (both the `Run` keybinding and the MCP dispatch sink hold `&mut Workspace` around the call), then re-derived its own `Entity<Workspace>` from a weak handle and called `.read(cx)`/`.update(cx, ..)` on it. Pre-existing since the 2026-06-24 re-fork. Invisible to the test suite because every test called `controller.run(..)` via `AnyWindowHandle::update` (bare `&mut App`, no lease) instead of the real `workspace.update(cx, ..)` shape. Fixed by making `run` an associated function that takes the caller's `&mut Workspace` + `Context<Workspace>` (and deleting the unused `rerun`) — **and** by moving `ConsolePanel::spawn_task` into the completion poller's async body, since it re-derives the workspace itself and panics under the lease even after the signature change. Read this before writing anything that runs under a `Workspace` lease. |
| 2026-08-17 | confirmed | [`findings/2026-08-17-gpui-draw-phase-invalidation.md`](findings/2026-08-17-gpui-draw-phase-invalidation.md) — **Two traps that make a correct UI fix a silent no-op.** (1) `Window::invalidate_view` **discards** an invalidation whose `draw_phase != None`, so a `cx.notify()` raised from `request_layout`/`prepaint`/`paint` pushes no effect at all — use `cx.defer`, guarded on the value having actually changed. `VisualTestContext::draw` skips `record_entities_accessed`, so a test on that path passes with either an inline or a deferred notify and proves nothing; drive the real `Window::draw`. (2) `workspace.screenshot` renders the **retained** scene and never runs prepaint — a screenshot cannot show a frame that was never drawn, so "still broken in three screenshots" is not evidence; nudge a real event first. Both bit this session: split-pane misalignment (`FORK.md` #62) and the image-viewer zoom readout. |
| 2026-07-15 | diagnosed (fix pending) | [`findings/2026-07-15-teammate-tab-lingering-subagentstop.md`](findings/2026-07-15-teammate-tab-lingering-subagentstop.md) — **Completed async-`Agent` teammate tabs linger ~1 h.** BUG + regression of the 2026-07-14 teammate-completion rework. Async `Agent` teammates close ONLY on a per-subagent Claude SDK `Stop` hook (`close_teammate_on_stop`), but that never fires: live log shows `end_of_turn=true` only ever with `agent_id=None` (Main), 0× across all 25 sub-agent ids. Sub-agent completion is Claude Code's `SubagentStop` event — **registered nowhere** (`build_default_hooks` wires only `Stop`+`PostToolUse`, `connection.rs:377-393`). The rework also removed the JSONL-`stop_reason` close trigger, so the only remaining close is the 3600 s live-parent reaper backstop (`store.rs:79`). Inline `Task` teammates unaffected (close on tool-call terminal). Fix dirs A/B/C in the doc; **awaiting user's choice — no fix or experiment started.** Related [`findings/2026-07-14-teammate-completion-signal-shipped.md`]. |
| 2026-07-14 | handoff | [`findings/2026-07-14-session-handoff.md`](findings/2026-07-14-session-handoff.md) — **READ FIRST on resume.** Shipped this session (all pushed): rename-session-purge fix (`2c57564fc1`), wire v6 + mobile numeric-id migration (`1679c533d0` + mobile `c58d88a`), the full teammate-completion architecture rework via authoritative Stop-hook (`677e0f3d55…67470c90be`, opus whole-branch review caught+fixed a #9 re-regression), worktree-hook "(deleted)" fix (`3b98c64da7`), follow-ups (`22ac330ac6`). `release-fast` rebuilt (18:02) + editor restarted (v6 confirmed live); mobile v6 APK on the phone. **IN PROGRESS (resume here):** brainstorming a fix for "restore all dialog tabs (AI + terminals) on Solution close→reopen" — grounded: it's ONE bug (terminals restore unconditionally in `console_panel::restore_from_db`; chat rows are skipped via the `session_exists` guard when the `solution_agent` store isn't hydrated yet), paused mid-read of the exact `active_solution_id_for_workspace`/`hydrate_open_tabs_lazy` root. |
| 2026-07-14 | shipped | [`findings/2026-07-14-teammate-completion-signal-shipped.md`](findings/2026-07-14-teammate-completion-signal-shipped.md) — **Teammate/subagent tab completion is now authoritative (root fix for the recurring zombie/stuck-tab class).** Instead of guessing "done" by tailing the agent's JSONL (a tab could stick `Live` ~1 h), the async `Agent` teammate closes on the Claude SDK `Stop` hook (already reaching the editor; `agent_id` == `BackgroundAgentId` → `parent_tool_use_id` → `close_stream`), inline `Task` closes on tool-call terminal, killed closes immediately; JSONL `stop_reason` dropped as a close trigger; reaper demoted to a lost-hook backstop whose live-parent window stays LONG (~3600 s — a live silent long tool call must not be mistaken for a lost hook, hardening #9). Spec `specs/2026-07-14-teammate-completion-signal-design.md`, plan `plans/2026-07-14-teammate-completion-signal.md`. Built subagent-driven (4 tasks + fixes, per-task + opus whole-branch review that caught & fixed an I-1/I-2 #9 re-regression); 589 tests green. |
| 2026-07-14 | fixed | [`findings/2026-07-14-mobile-wire-numeric-ids-v6.md`](findings/2026-07-14-mobile-wire-numeric-ids-v6.md) — **Mobile client crash-decoded numeric ids; wire bumped to v6.** The identity migration made Solution/member/catalog ids `i64` (JSON numbers on the wire) but never bumped `wire_schema_version`, and the mobile DTOs still declared them `String` → kotlinx.serialization crash `"String literal for value of key 'id' should be quoted at path: $.id"`. **FIXED lock-step:** editor `wire_schema_version` 5→6 (`1679c533d0`); `spk-editor-mobile` migrated every Solution/catalog id `String`→`Long` across DTOs/VMs/UI/CLI + `SUPPORTED_WIRE_SCHEMA_VERSION`=6 + `CACHE_SCHEMA_VERSION` bump (`c58d88a`, not pushed yet). Session/agent ids stay `String`. Verified: editor e2e green; mobile `:core:test` 332/0, `:app:testDebugUnitTest` 41/0; new regression `workspace_solution_decodes_numeric_ids`. **v6 editor + v6 mobile must ship together.** |
| 2026-07-14 | fixed | [`findings/2026-07-14-rename-purges-open-sessions.md`](findings/2026-07-14-rename-purges-open-sessions.md) — **Renaming a Solution hard-purged its open AI sessions** (second data-loss bug in the folder-move rename arc). `on_solution_event` runs `gc_orphan_members` on every `SolutionStoreEvent::Changed`, which hard-purges any hydrated session whose in-memory `cwd` isn't the solution root or under a live member. A folder-moving `rename_solution`/`rename_member` rewrites the store's root/member paths to the **new** location and emits `Changed`, but live sessions keep their **old-root** cwd (the DB cwds are only reconciled at the next startup) → every open session is a false orphan and is deleted. **FIXED**: new `SolutionStoreEvent::PathsMoved { id, old_prefix, new_prefix }` emitted **before** `Changed` from both rename paths; `SolutionAgentStore::rewrite_session_cwds_for_move` fixes the live cwd (in memory, so the trailing gc sees it valid) and the cold ones (`SolutionAgentDb::rewrite_session_cwds` in the DB, so a same-process reopen doesn't re-hydrate a stale cwd into the gc). Three regression tests + `gc_orphan_members` unchanged; 584+220+e2e green. Already-purged sessions are not recoverable in-app. |
| 2026-07-13 | shipped | [`findings/2026-07-13-rename-with-folder-move-shipped.md`](findings/2026-07-13-rename-with-folder-move-shipped.md) — **READ FIRST on resume** (with [`findings/2026-07-13-session-handoff.md`](findings/2026-07-13-session-handoff.md)). The three-plan rename arc is implemented, verified end-to-end and pushed: surrogate counter ids (FORK.md #50), rename moves the folder via a hot `rename(2)` + compat symlink and a cold startup reconcile (#52), editor-owned claude settings + worktree hook (#51). Real-DB rehearsal numbers (176 sessions preserved / 169 remapped / 70 `member_id`s backfilled / 0 dangling `active_member` rows) — **the operator's real databases migrate on the next launch of the new binary**; pre-migration backups at `/tmp/db.sqlite.pre-identity-1783948879` + `/tmp/solution_agent.db.pre-identity-1783948879`. Open follow-ups: `file_folds` is missing from the cold rewriter (a fold is orphaned by a rename); the two `editor_mcp/tests/solutions_e2e_test.rs` tests collide on the process-global runtime-dir `OnceLock` + `SingleInstanceLock` (each passes alone, the pair fails); `workspace.list_buffers` on a per-solution socket reports another Solution's buffers when both are open in one window. |
| 2026-07-13 | fixed (gap open) | [`findings/2026-07-13-rename-solution-cascade-data-loss.md`](findings/2026-07-13-rename-solution-cascade-data-loss.md) — **Renaming a Solution cascade-deleted its members.** `solutions::db::save_solution` used `INSERT OR REPLACE INTO solutions`; `solution_members`/`active_member` reference `solutions(id) ON DELETE CASCADE` with `PRAGMA foreign_keys=TRUE` global, so SQLite's REPLACE deleted-then-reinserted the parent row and the cascade silently wiped every member row + the active member on rename (`store/lifecycle.rs::rename_solution` was the only re-save-existing-id call site) — invisible until the next cold load showed "0 projects". **FIXED** (`132e89f5a7`): `save_solution` is now `INSERT ... ON CONFLICT(id) DO UPDATE SET` (updates in place, never deletes); new regression test `db::tests::resaving_solution_preserves_members_and_active_member`; audited siblings (`set_solution_member`/`set_active_member`/`save_catalog_project` are leaf tables, safe). User's live DB hand-repaired (backup at `/tmp/db.sqlite.bak-1783929090`). **Open gap**: the empty-solution "can't create a chat" guard (`console_panel::panel::workspace_has_worktree`) checks `worktrees()` not `visible_worktrees()`, so an invisible-worktree-only empty solution still passes it and a chat lands at cwd `ROOT`; `solution_agent.create_session` MCP path has no `members.is_empty()` check at all. |
| 2026-07-07 | handoff | [`findings/2026-07-07-session-handoff-4.md`](findings/2026-07-07-session-handoff-4.md) — **READ FIRST on resume.** Post phase 6d-A. **6d-A DONE + SHIPPED** (`ed335daa49`): background SHELLS fold into `session.streams` as auto-closing `StreamId::Shell` tabs (only while `Running` — dismissible terminal-× UX dropped per user); cx-free `stream_entry`/`stream_label` injected in `rebuild_streams` from `background_shells`; desktop renders shell pill + body from streams (unified path); WIRE-INVISIBLE (`build_streams_vec` filters Shell, Shell `stream_id`→Main) so v3 wire byte-identical, mobile untouched. 553 tests, screenshot gate passed, release-fast rebuilt. **NEXT = 6d-B** (CROSS-REPO: fold background AGENTS onto their demux `Teammate` stream [JSONL→signal/archival, content-reduction USER-APPROVED] + remove Shell wire filter + the separate `get_session_background_{shells,agents}` tools + `wire_schema_version` 3→4 + `sawe-mobile` lockstep [delete `BackgroundShellStrip`/`BackgroundAgentStrip`+RPCs/notifications, fold into `SubagentTabStrip`, bump gate, Shell Roborazzi fixture] + emulator gate; **coordinated push needs a one-line mobile-push confirm**) → 6d-tail (`SubagentView`→`StreamId` collapse + field removal) → 6e. Supersedes below. |
| 2026-07-07 | fixed | [`findings/2026-07-07-phase6d-A-shells-into-streams.md`](findings/2026-07-07-phase6d-A-shells-into-streams.md) — **Phase 6d-A SHIPPED** (`ed335daa49`). Background shells → `session.streams` as auto-closing `StreamId::Shell` tabs (exist only while `Running`; terminal-× UX dropped). cx-free `BackgroundShell::stream_entry`/`stream_label`, injected in `rebuild_streams` (called at all 5 shell-mutation sites); `SubagentView::Shell.parent_stream_id()`→`Some(StreamId::Shell)` so the shell body renders through the same unified path as Main/teammates (deleted the map-based shell strip loop + `build_background_shell_entries_for_render` + `build_shell_drill_in_entries`). WIRE-INVISIBLE: `build_streams_vec` filters `StreamKind::Shell` + Shell `stream_id`→Main, so the v3 wire is byte-identical and mobile is untouched (that fold is 6d-B). 553 tests; offscreen screenshot gate passed (shell pill + fenced-output body, view-only). |
| 2026-07-06 | handoff | [`findings/2026-07-06-session-handoff-3.md`](findings/2026-07-06-session-handoff-3.md) — Post phase 6c. **Phase 6c DONE + SHIPPED** (`7aeeee7470` code, `7144dc94d2` seed-gate tool): the DESKTOP tab strip teammate loop + `next_selection_after_change` read `session.streams` instead of `active_subagent_order` (snap-to-Main only on stream removal). STAGED teammates-only — `SubagentView` variants + `active_subagents*`/`background_*_order` KEPT (still wire-backing `SessionSummary` + bg-agent/shell drill-in until 6d). Fixed while here: **decision-#16 latent-prod bug** (`hydrate_streams_main_only` read STALE `self.streams` on cold-load → decision-#9 zombie teammate tabs; now demuxes `entries`) + **review-caught →Idle-GC regression** (cleared `active_subagents` without closing the stream → frozen tab; GC now `close_stream`s). 558 tests, offscreen strip screenshot gate PASSED (Main+teammate+after-close), release-fast rebuilt. **NEXT = 6d** (shells/bg-agents into `streams`, cross-repo wire bump 3→4 + `sawe-mobile` lockstep + emulator gate; THEN the full `SubagentView`→`StreamId` collapse + order-vec removal become clean) → 6e (final docs + whole-branch review). Supersedes the row below. |
| 2026-07-06 | fixed | [`findings/2026-07-06-phase6c-desktop-strip-streams.md`](findings/2026-07-06-phase6c-desktop-strip-streams.md) — **Phase 6c SHIPPED** (`7aeeee7470`). Desktop strip teammate (`tabs`) loop iterates `session.streams` (filtered `∈ active_subagents` — the behavior-preserving bridge that excludes async-`Agent` teammate streams so they aren't double-pilled), `next_selection_after_change` takes `&session.streams` (snap-to-Main only on stream removal). Teammates-only staging: fields + `SubagentView` variants KEPT for the wire + bg drill-in (removal = 6d with the wire bump; `StreamId` has no `Background`, async teammates double-represented). Two bugs fixed: **(A)** decision-#16 cold-load orphan bug — orphans were read from the stale `self.streams` mirror (cold-load assigns `entries` with no rebuild first) → decision-#9 zombie tabs after restart; now `demux(&self.entries)`. **(B)** review-caught: the →Idle strip GC cleared `active_subagents` without `close_stream` → a viewer stranded on a frozen tab under the new streams-only snap; GC now closes each cleared teammate. 558 tests + 2 regressions; offscreen screenshot gate passed. |
| 2026-07-06 | handoff | [`findings/2026-07-06-session-handoff-2.md`](findings/2026-07-06-session-handoff-2.md) — Post phase 6b. **Phase 6b (keystone) DONE + SHIPPED** (`306ca1af5f` code, `fdeb268f06` docs): persist authority → `streams[Main]` (Main-local, seq-watermark, per-session persist serialized against the non-FIFO detached-write race; legacy rows realign on cold-load), quick-fix #3 REVERTED, decision-#11 re-stamp re-homed to Main, flat `entries` kept as ingest mirror. 556 tests, render gate re-passed, release-fast rebuilt. **NEXT = 6c** (desktop strip onto `streams`, drop `SubagentView`/order-vecs) → 6d (shells/bg-agents into `streams`, cross-repo wire bump 3→4) → 6e (final docs + whole-branch review). Open follow-up: verify `hydration_orphan_streams` records on the real cold-load path. Supersedes the row below. |
| 2026-07-06 | fixed | [`findings/2026-07-06-phase6b-retire-flat-entries.md`](findings/2026-07-06-phase6b-retire-flat-entries.md) — **Phase 6b (keystone) SHIPPED** (`306ca1af5f`). Persistence authority moved from flat `session.entries` to `streams[Main]` (Main-local index, seq-watermark incremental persist); **quick-fix #3 REVERTED** (`AcpThread` back to naive `entries.last()`; demux owns un-tear). Flat `entries` KEPT as the 1:1 `AcpThread` ingest mirror. Two review-caught bugs fixed: **(1)** append-race — unconditional `delete_entries_from` + GPUI's non-FIFO detached writes dropped a just-written row → **serialized per-session persist** (`entries_persist_chain`); **(2)** legacy global-index rows not realigned on cold-load → phantom teammate row / lost Main entry → **realign** (seed watermark 0 when flat mirror longer than Main). 556 solution_agent tests; live torn-message render gate re-passed offscreen. Open follow-up flagged: `hydration_orphan_streams` may be empty on real cold-load (orphan set read pre-rebuild) — verify in 6c/6d. **NEXT = 6c** (desktop strip onto `streams`). |
| 2026-07-06 | handoff | [`findings/2026-07-06-session-handoff.md`](findings/2026-07-06-session-handoff.md) — **READ FIRST on resume.** **Per-source-streams phases 4+5 SHIPPED in lockstep** (hard cutover, `wire_schema_version` 3). Server (`sawe` `origin/main` ≈`01d5c36bbe`, 554 tests): `get_session`/`get_session_changes` serve `session.streams` (descriptors-all + entries-for-selected `stream_id`, stream-local index, per-stream `seq` delta); deferred phase-3 items closed (async-Agent close, hydration orphans). Mobile (`sawe-mobile` `origin/main` `dc1977d`): DTOs mirror server, filter deleted, tabs from `streams`, wire-gate v3 + too-old reject; render verified offscreen (Roborazzi). **NEXT = phase 6 cleanup ONLY**: revert quick-fix #3 (`AcpThread::coalesce_target_index`, FORK.md #39 — delicate), delete #1/#2, retire flat `entries`, remove `SubagentView`/order-vecs/bg-agent tab dup, unify shells/bg-agents into `streams`. Review-gated. |
| 2026-07-06 | fixed | [`findings/2026-07-06-phase4a-server-model.md`](findings/2026-07-06-phase4a-server-model.md) / [`4b`](findings/2026-07-06-phase4b-server-wire.md) / [`5`](findings/2026-07-06-phase5-mobile-streams.md) — per-source-streams phases 4a/4b/5 detail (seq mechanism, two-overlay hydration, async-Agent close, wire descriptors+selected-entries, coalesce-split delta-miss fix, mobile cutover). Superseded index row above. |
| 2026-07-06 | fixed | [`findings/2026-07-06-torn-message-interleaved-subagent-chunks.md`](findings/2026-07-06-torn-message-interleaved-subagent-chunks.md) — **A main-agent message rendered as many bubbles, split even mid-word** (Main + mobile). A claude async `Agent` teammate streams into the PARENT thread concurrently, so the parent's own text deltas arrive interleaved with the teammate's `subagent_id`-tagged chunks; `AcpThread` coalesced onto `entries.last()`, which after an interleaved subagent chunk was the subagent entry → the parent's next delta started a fresh `AssistantMessage`. The sub-agent tab looked clean because it sources from the teammate's JSONL, not the parent thread. **FIXED**: `AcpThread::coalesce_target_index` scans back skipping *other* sources' entries, stopping at the source's own tool call / structural entries; both streaming paths emit `EntryUpdated(target_idx)` so the store re-syncs the right entry to mobile. FORK.md #39, acp_thread 82 (+2) / solution_agent 525. |
| 2026-07-06 | fixed | [`findings/2026-07-06-subagent-entries-flood-main-after-finish.md`](findings/2026-07-06-subagent-entries-flood-main-after-finish.md) — **An actively-streaming async `Agent` teammate showed no strip tab and flooded Main** (desktop + mobile). Two linked bugs: **(A)** the background-agent pill registers by parsing the `agentId:`/`output_file:` announcement from the terminal `Agent` call's `raw_output` — but for an async launch `raw_output` is null and the announcement is in the tool_result **content**, so parse failed → no pill (Task pill already removed on the early spawn-ack completion). **(B)** Main's sub-agent filter was *bypassed* when `active_subagents` was empty (desktop `should_render_entry`, mobile `ChatList`, a "cold-restart guard"); a background agent leaves that map empty, so tagged output flooded Main the whole run. **FIXED**: (A) `managed_agent_announcement(raw_output, content)` falls back to content, lifecycle snapshot captures it; (B) dropped the bypass — Main is always main-thread-only. Both needed (B alone → teammate invisible; A alone → still floods). FORK.md #38, editor 525 + mobile compile/`:core:test` green. |
| 2026-07-05 | fixed | [`findings/2026-07-05-observer-cant-see-agent-answer.md`](findings/2026-07-05-observer-cant-see-agent-answer.md) — **The observer never saw the agent's ANSWER, and anchored on its own nudges** → it re-flagged an already-delivered directive across 4 wake-ups (the agent re-explained ~3×). `apply_user_anchored_filter` kept only the entries *before* each user message + the resting turn, so a reply the agent gave then worked past was invisible; and a supervisor nudge (delivered as a `role==user` entry with only the `spk_observer_nudge` `_meta` marker) was anchored on as if it were a fresh user goal. **FIXED**: new `EntrySummary.observer_nudge` (via `is_observer_nudge_blocks`) → judge anchors on `role==User && !observer_nudge` + a **trail** keeps up to 5 assistant text turns (the answer) after each anchor, tool calls skipped, stopping at the next user (no overlap); judge instructions updated. Same DTO field fixes the **mobile** Observer plaque (nudge was `role==User` → rendered as a plain user bubble). Plus: manual `/clear` + `/compact` now **wipe the observer's memory** (`supervisor::wipe_supervisor_memory`, gated by `CompactInitiator`) so a looping observer can be reset; the observer's own `compact` verdict keeps `user_intent.md`. FORK.md #37, editor + mobile tests green. |
| 2026-07-04 | fixed | [`findings/2026-07-04-supervisor-trigger-audit.md`](findings/2026-07-04-supervisor-trigger-audit.md) — full audit of the observer TRIGGER (my read + an independent adversarial agent). Fixed: (1) a dropped verdict left status pinned `Judging` → bogus backoff → false `Stopped(ProviderError)`; (3) a parked `pending_nudge` flushed even after the user hit Stop; (4) background-agent completion didn't reset the silence clock; (5) a scheduled auto-resume (`next_eligible_ms`) was gated out forever by `watch_started_ms` after a restart; (6) defensive `last_fired_at=None` watchdog. Deferred (narrow): (2) `spawn_judge` no-project bail leaves `Judging` orphaned — needs a fire-precondition + test-harness work. Suite 518. FORK.md #31/#32/#33. |
| 2026-07-04 | shipped | [`findings/2026-07-04-notification-quiescence-and-click-focus.md`](findings/2026-07-04-notification-quiescence-and-click-focus.md) — desktop notification rework: (1) "agent finished" (`Completed`) now fires only when truly quiescent — `decide_notification` also suppresses on live background work and an auto-driving supervisor (was: pending-queue only); (2) click-to-focus — `notifier::dispatch` stamps `default_action("open")`, and a new `zed::notification_focus` portal listener routes a click (id `dev.sawe.session-{sid}`) to raise the window + activate the Solution + focus the ConsolePanel + select the session tab. FORK.md #36. Portal click→focus is manual-verify (no headless portal). |
| 2026-07-04 | fixed | [`findings/2026-07-04-orphan-result-drops-turn-tail.md`](findings/2026-07-04-orphan-result-drops-turn-tail.md) — **Mobile: final turn message lost when the turn ends while the dialog is closed** (recurring). Reopening shows the last intermediate step + Idle; even a full `get_session` is stale, so the tail is genuinely absent server-side. Root cause: `claude_native`'s **orphan-result** turn-end (a terminating `result` with no `prompt()` in flight — e.g. a background-bash continuation) synthesizes `Stopped`/`Error` directly on the thread **without the end-of-turn tail flush**, so the follow-up message's final bytes never become an `EntryUpdated` → never funnel into `session.entries` → no `mod_seq` bump → invisible to every mobile catch-up path (which all read `session.entries`). Idle still propagates → "stale step + Idle". **FIXED**: new `AcpThread::flush_end_of_turn_tail` (flush + `EntryUpdated(last)`) called by the mainline arms AND the orphan path; store `Error` arm now flushes pending appends like `Stopped` (shared `flush_pending_entry_appends`). FORK.md #35, 2 new tests. |
| 2026-07-04 | fixed | [`findings/2026-07-04-stuck-watchdog-usage-limit-loop.md`](findings/2026-07-04-stuck-watchdog-usage-limit-loop.md) — **The hang watchdog reconnect-looped on a usage/session-limit wall.** A turn that hit `You've hit your session limit` prints the wall and stalls `Running` — which `tick_stuck_sessions` read as a hung subprocess, so it reconnected + sent "твой процесс завис… продолжай", which re-hit the wall → loop (quota burn + spam). **FIXED**: the watchdog now scans the latest assistant message; a `is_usage_limit_error` match is routed to a shared `apply_usage_limit_stop` (stop turn as `Errored(<wall>)`, `system` note, auto-resume at reset if supervised else `Stopped(Quota)`) instead of reconnecting. Helper extracted from `on_judge_failed`'s Quota arm so judge-wall and agent-wall share one handler. FORK.md #34, 1 new test, suite 510/510. |
| 2026-07-04 | fixed | [`findings/2026-07-04-supervisor-restart-state-reconciliation.md`](findings/2026-07-04-supervisor-restart-state-reconciliation.md) — **Supervisor in-flight state isn't reconciled on restart.** Two live-reported bugs, one root: the supervisor persists its `status`/`last_fired_at` row but the judge lives only in the transient `judge_sessions` map. (1) Reopening the editor **fired a judge instantly** on a session that was idle before the restart (stale `last_activity_at` ⇒ `should_fire` sees "silent for hours"). (2) A row persisted mid-`Judging` restored as a **phantom "reviewing" that hangs** — no judge running, so `supersede_judge_on_user_reply` no-ops on the user's kick and the stuck-watchdog may never trip. **FIXED**: `load_supervisor_states` coerces phantom `Judging → Watching` (drops stale `last_fired_at`); new transient `SupervisorState.watch_started_ms` stamped by `set_persistence` makes `tick_supervisor` fire only on activity *after* this process started watching — so after a restart nothing auto-resumes, each session waits for a manual kick (operator's explicit rule); fresh in-session enables leave it `None` → unchanged. FORK.md #33, 2 new tests + 1 extended. |
| 2026-07-02 | fixed | [`findings/2026-07-02-supervisor-send-time-state-audit.md`](findings/2026-07-02-supervisor-send-time-state-audit.md) — **Supervisor state was checked only at judge START, never at SEND.** Now double-checked: `apply_verdict` drops a verdict when supervision is no longer running (`!enabled`/`Held`/`Stopped`), and known-stale runs are **interrupted** (disable → `finish_judge`+`finish_auditor`; instruction change → tear down + re-arm) instead of running to completion. **Hold-on-typing**: a nudge whose judge finished while the user is composing is parked in `SupervisorState.pending_nudge` and flushed once the user goes quiet for the idle window; a user SEND discards it. Status-row eye **pulses** while `Judging`. Fixes: disabling didn't stop a running observer; a verdict racing in after Stop still nudged. **Follow-up (#32): the judge no longer polls** — no fire while a background command/agent is running; `wait` is one-shot (`MAX_WAIT_SECS` 300→1800, parked in `wait_until_ms`, mechanism wakes the agent at the deadline, no re-judging); operator-blocked → `done`/`ask` not `wait` (instructions). Root cause: one session logged 89 identical `wait` verdicts polling a parked agent. FORK.md #31+#32, 7 new tests, suite 507/507. |
| 2026-07-02 | fixed | [`findings/2026-07-02-newchat-action-double-lease.md`](findings/2026-07-02-newchat-action-double-lease.md) — dispatching the `console_panel::NewChat` **action** with an active Solution double-lease-panicked (`add_chat_tab_with_cwd` re-read the `Workspace` entity inside the action's `Workspace` update). **FIXED**: `add_chat_tab{,_with_cwd}` now take `project: Entity<Project>` from the caller (the handler passes `workspace.project().clone()`; the "+" menu reads it at render time) instead of re-leasing the Workspace. Deferring doesn't help (a deferred closure still leases Workspace). Verified live: no panic, session created. |
| 2026-06-27 | handoff | [`findings/2026-06-27-session-handoff-phase6.md`](findings/2026-06-27-session-handoff-phase6.md) — **READ FIRST on resume (mobile-delta-sync).** **Phase 6 (Kotlin mobile client + rebrand) CODE-COMPLETE** in the `spk-editor-mobile` repo, branch `phase6-mobile-delta-sync` @ `1c5f064`, **NOT pushed / NOT merged**. Rebrand "SPK Remote"→"Sawe Mobile" (`ru.sipaha.sawe.*`, legacy `spk-editor-remote://` alias removed; pairing protocol UNTOUCHED). Chat sync rebuilt: cache-first **single-writer** delta client on `get_session_changes` — `:core` DTOs + pure `applySessionDelta` (drop-by-COUNT tail-truncate, absent-vs-empty sections), `(epoch,lastSeq)` cache cursor + schemaVersion-2 legacy gate (a Critical `encodeDefaults=false` gate bug was found+fixed), `SessionDetailStore` rewrite (push handlers → debounced poll; old after_index/per-entry-refetch machinery deleted). 8 commits off mobile `40b3b7d`; per-task + whole-branch opus reviews clean after fixes; `:core` 344 / `:app` 31 green. **OUTSTANDING: user device-verification** (queued-bubble stable / scroll holds / `/clear` reloads empty / desktop in-place edit propagates), then merge→mobile `main`. Accepted gap: EncryptedSharedPreferences IO not auto-tested (Robolectric Keystore). |
| 2026-06-26 | handoff | [`findings/2026-06-26-session-handoff-phase5.md`](findings/2026-06-26-session-handoff-phase5.md) — **(Phase 5 — superseded by the Phase 6 handoff above.)** **Phase 5 COMPLETE & offscreen-verified**: the delta RPC `get_session_changes` (changed entries by `mod_seq > since_seq` + filter, sections gated on per-section watermarks `queue_seq`/`subagents_seq`/`state_seq`, `reset` on epoch mismatch). `change_seq` is now PERSISTED + restored as the watermark anchor (fixes a latent lost-entry bug after desktop restart); `GetSessionResult` seeds `epoch`+`current_seq`; tool wired into `GLOBAL_TOOLS`/`allow_list` for mobile reach. 6 commits `f831cb5975..9e4cb7deaa`, 412+ tests, whole-phase opus review clean. **Merged to `main` + pushed to `origin/main` (`..d6ec340db4`).** **NEXT: Phase 6 — Kotlin mobile client (`spk-editor-mobile`) + app-identity rebrand → "Sawe Mobile" / `ru.sipaha.sawe.*`.** Gotcha: screenshot tool is `windows.screenshot {window_id}`. |
| 2026-06-26 | handoff | [`findings/2026-06-26-session-handoff.md`](findings/2026-06-26-session-handoff.md) — **(Phase 4 — superseded by the Phase 5 handoff above.)** **Phase 4 COMPLETE & offscreen-verified**: transcript moved from a JSON blob to per-entry `solution_session_entries` rows + lazy blob→rows migration (incl legacy v1) + epoch/mod_seq deltas. 9 commits `d88778b77f..f831cb5975` on `main` (NOT pushed), 397 tests. New Task 3a moved model/effort/cached_models to metadata columns; Task 5 split into 5a (repoint MCP get_session/read_session_history to `session.entries` — fixes a cold-session-empty regression Task 4 introduced + Phase-5 groundwork) + 5b (delete dead blob-write machinery, keep blob read path for migration). Live migration verified on real legacy dev sessions (rows written, epoch set, blob preserved, render paints). **NEXT: Phase 5 — RPC `get_session_changes` delta**, then Phase 6 Kotlin client. Gotcha: screenshot tool is `windows.screenshot {window_id}` in this build. |
| 2026-05-29 | handoff | [`findings/2026-05-29-v3-shells-session-handoff.md`](findings/2026-05-29-v3-shells-session-handoff.md) — **READ FIRST on session resume.** V3 **Background Shells Strip** shipped end-to-end + polish (`7752146ae2..9562fdc580`, 14 tasks + V3-polish-1, 324 `solution_agent` tests, clippy-clean via `--no-deps`). Mid-flight the plan's premise was disproven — background-shell stdout is an on-disk `/tmp/claude-*/.../tasks/<id>.output` file (path in the launch announcement) + a `<task-notification>` completion message, NOT memory-only tool_calls — so the feature was rewritten to mirror Managed Agents (parse launch → tail file → terminal). Live `Exited(code)` works by tailing the parent session JSONL for `<task-notification>` on the 1Hz tick (NOT via thread entries — a NewEntry would stick the session on Running). KillShell→Killed is live. Next pick: V3-polish-2 — clear pre-existing workspace clippy debt (claude_native/solutions) so clippy runs without `--no-deps`. |
| 2026-05-29 | finding | [`findings/2026-05-29-background-shell-real-shapes.md`](findings/2026-05-29-background-shell-real-shapes.md) — verified real shapes for Claude Code background shells: launch announcement (`Command running in background with ID: <tok>. Output is being written to: …/tasks/<tok>.output`), the on-disk `.output` file, and the `<task-notification>` completion block (with `(exit code N)`). Disproves the original V3 Constraint #1. |
| 2026-05-29 | superseded | [`findings/2026-05-29-session-handoff.md`](findings/2026-05-29-session-handoff.md) — **(superseded by the V3 handoff above.)** Shipped: V2 polish for Background Agents strip (settings keys, mtime+size cache, incremental tail, thinking blocks, tool_result content+Failed), Opus 4.7→4.8 bump, live `active_model` in status row, click-to-wake Sleeping badge, tool_call arg preview in conversation header. Total 10 commits since V1 handoff (`5af1b48155..0855f2b003`), 254 tests passing. **Outstanding: V3 background-shells-strip** — 14-task plan at [`superpowers/plans/2026-05-29-background-shells-strip.md`](superpowers/plans/2026-05-29-background-shells-strip.md), subagent-driven-development ready. Hard constraint: no FS for shell buffers (claude code holds pipes in memory) — state from observed tool_calls only. |
| 2026-05-28 | shipped | [`findings/2026-05-28-background-agents-strip-handoff.md`](findings/2026-05-28-background-agents-strip-handoff.md) — Background Agents Strip V1 (Tasks 1-13 of 14) shipped to `main` (`8ae4b046..0084dbbf99`, 19 commits, 248 tests). Managed Agents pills + JSONL view + SQLite + reconciliation. Task 14 (manual smoke with real claude subprocess) deferred. Same handoff lists 6 V2 polish items — **all 5 of those that mattered landed in the 2026-05-29 session above; the 6th (`fs.watch` before Agent return) was declined as not-needed**. |
| 2026-05-27 | handoff | [`findings/2026-05-27-session-handoff.md`](findings/2026-05-27-session-handoff.md) — Shipped this session: New AI Chat root picker (flat menu — GPUI `ContextMenu::submenu` click broken in our setup, see gotcha), `SessionSummary.cwd` MCP exposure, per-turn git checkpoint disable (FORK.md #23). In-flight: **background-agents-strip** — spec + plan written, 14-task plan ready for subagent-driven execution. Branch state: was on `feature/unified-workspace-wire` (parallel session built `workspace_events` crate on top of our work), `Cargo.lock` had uncommitted diff — supervisor must resolve branch + lock before Task 1. Superseded by 2026-05-28 (V1 shipped) and 2026-05-29 (V2 polish + V3 plan). |
| 2026-05-26 | shipped | [`findings/2026-05-26-console-panel-shipped/`](findings/2026-05-26-console-panel-shipped/) — ConsolePanel arc shipped (B8 popover, B9 context menus, B10 persistence, B11 navigator refactor + terminal-factory port + workspace wireup, B12 settings cleanup, B13 docs). Live-verified screenshots: empty panel, terminal + chat tabs, restored after restart. See [`README.md`](findings/2026-05-26-console-panel-shipped/README.md) in the folder. |
| 2026-05-26 | handoff | [`findings/2026-05-26-session-handoff-evening.md`](findings/2026-05-26-session-handoff-evening.md) — **READ FIRST on session resume (ConsolePanel arc on branch `hook-inject`).** Supersedes the morning handoff. **Phase B complete (15/15)** — feature live-verified, persisted, tested. |
| 2026-05-26 | blocker | [`findings/2026-05-26-b11-blocker.md`](findings/2026-05-26-b11-blocker.md) — Why B11 isn't mechanical: `SolutionSessionView` structurally embeds `WeakEntity<SolutionSessionsNavigator>` and calls `render_status_row` from its own render; plus `TerminalPanel::add_center_terminal` / `new_terminal` are static factories with ~32 external callsites that take `SpawnInTerminal` / `TaskState` / `RevealStrategy`. Three options (A/B/C) with effort + risk. |
| 2026-05-26 | superseded | [`findings/2026-05-26-session-handoff.md`](findings/2026-05-26-session-handoff.md) — **Morning snapshot at B7/15.** Phase A (Right Dock full-height, BottomDockLayout removed) shipped end-to-end with snapshot test. Phase B (unified bottom-dock panel merging Terminal + AI-chat) at 7 / 15: crate scaffold, IconName::Console + SVG, ConsolePanelSettings, TerminalProvider, ChatProvider (incl. SessionCreated subscription), ConsolePanel skeleton + impl Panel, tab-strip render (heterogeneous icons). 13 commits. Outstanding pool: B8 (+ popover) → B9 (ctx menu) → B10 (persistence, HEAVY) → B11 (wire-up + delete old TerminalPanel/Navigator, HEAVY) → B12-B15 (cleanup / docs / e2e / screenshots). Distinct from 2026-05-22 claude_native arc (different branch). |
| 2026-05-22 | handoff | [`findings/2026-05-22-session-handoff.md`](findings/2026-05-22-session-handoff.md) — **READ FIRST on session resume.** Native `claude_native` crate replacing the `claude-agent-acp` node wrapper (fixes the server-side stuck-`Running` hang via deterministic `result` parsing + two-stage Stop + 15-min watchdog). Branch `claude-native-foundation`, 8 commits, Phases 0-3 done (pure layers: protocol/command/translate, 24 tests green); Phases 4-8 (process I/O, connection+prompt, permission bridge, cancel, watchdog, close) outstanding. Spec+plan under gitignored `docs/superpowers/`. Subagents were 529-down all session. Related but distinct: 2026-05-18 mobile state-pill bug below. |
| 2026-05-18 | open-bug | [`findings/2026-05-18-mobile-session-state-stuck-running.md`](findings/2026-05-18-mobile-session-state-stuck-running.md) — Mobile chat-header state pill stays at `Running` after backgrounded `Running→Idle` transition. Hypothesis: WS subscription gap during background or VM reducer treats replayed notification as stale. Quick mitigation: re-issue `get_session` on onResume. Deferred to next mobile-side phase. |
| 2026-05-17 | handoff | [`findings/2026-05-17-session-handoff.md`](findings/2026-05-17-session-handoff.md) — **READ FIRST on session resume.** Supersedes 2026-05-16 handoff. R-5 + R-6 arcs closed end-to-end: Android client is prod-shippable (release APK 2.12 MB, persists pairing, survives flaky network, rich chat with images + tool-calls). Remaining pool: R-6c (FCM + multi-server), outbound-queue disk persistence, crash reporting, F (sub-agent indication UI in sawe), G (spk-image:// repro). |
| 2026-05-16 | superseded | [`findings/2026-05-16-session-handoff.md`](findings/2026-05-16-session-handoff.md) — Supervisor-session handoff at the close of 2026-05-16. R-5a–d shipped + R-5e server enrichment. F/G outstanding-pool entries updated after cockpit audit found they belong in sawe, not cockpit. |
| 2026-05-15 | superseded | [`findings/2026-05-15-session-handoff.md`](findings/2026-05-15-session-handoff.md) — Original supervisor-session handoff. Listed pool items (E, R-2..R-6, F, G) that have since shipped or moved out-of-tree. See 2026-05-16 handoff for authoritative state. |
| 2026-05-17 | upstream-gap | [`findings/2026-05-17-remote-control-r6f-upstream-gap.md`](findings/2026-05-17-remote-control-r6f-upstream-gap.md) — R-6f WS compression cancelled: pinned `tokio-tungstenite 0.28` + latest 0.29 + underlying `tungstenite` do not implement `permessage-deflate` (verified from cached sources — no Cargo feature, no `WebSocketConfig` field, no extensions API). Re-open when upstream lands it. |
| 2026-05 | gotcha | [`findings/2026-05-remote-control-r4-mcp-envelope.md`](findings/2026-05-remote-control-r4-mcp-envelope.md) — Embedded `editor_mcp` over Unix socket needs `tools/call { name, arguments }` envelope; bare `{"method": ..}` returns -32601. Discovered building R-4 proxy. |
| 2026-05 | gotcha | [`findings/2026-05-remote-control-watcher-echo.md`](findings/2026-05-remote-control-watcher-echo.md) — `remote_control::store` FS-watcher echo loop on settings writes; resolution `self_write_echoes` counter. |
| 2026-05 | active | [`findings/2026-05-agent-worktree-staleness.md`](findings/2026-05-agent-worktree-staleness.md) — Agent-tool `isolation: "worktree"` branches from session-start HEAD, NOT current HEAD; freshly-committed plan-docs aren't visible to sub-agents in the same session |
| 2026-05 | resolved | [`findings/2026-05-headless-screenshot-blank.md`](findings/2026-05-headless-screenshot-blank.md) — `workspace.screenshot` returns blank under `--headless` (Xvfb); resolved by ADR-0002 (native headless platform) |

---

## Module docs (`architecture/modules/<crate>.md`)

Per-crate documentation of public API + invariants + pitfalls. Created on
first non-trivial public API in a crate; updated when that API changes.

(None yet — the first fork-owned crate to get one will be one of
`solutions` / `solution_agent` / `solutions_ui` / `editor_mcp` since those
are where the fork's public surface lives.)

---

## What does NOT go here

- **Status updates** ("Phase 3 in progress, blocked on X") — `git log` and the
  plan-doc status field hold this.
- **Per-crate module layouts** ("crate X has modules a/b/c") — `cargo doc` /
  reading the code is faster than maintaining this in docs.
- **mdBook user-facing docs** — those live in `docs/src/` and follow
  `docs/AGENTS.md`. The supervisor workflow does not touch them.
- **Rebrand spec & locked identifiers** — those are in `CLAUDE.md` (always-in-context).
- **What's-disabled list** — also in `CLAUDE.md`.
